[package]
name = "aw-speech"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Speech output for AccessiWeather (worker thread over a platform TTS sink)"

[features]
default = ["native"]
native = ["dep:tts"]

[dependencies]
tracing.workspace = true
tts = { workspace = true, optional = true }
